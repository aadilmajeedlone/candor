export const MAX_FILE_BYTES = 15 * 1024 * 1024;
export const MAX_TEXT_CHARS = 120_000;
/** A résumé longer than this is almost certainly not a résumé; we cap what we read. */
export const MAX_PDF_PAGES = 40;
